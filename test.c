int main(void) {
    return
        (
                    /* heavy paws of lead  */
            (7 * (3 + 2) > 20 && /* fill his victims full of ... */
             (5 <= 5 || 1)) || 

            !(4 - 2 * 3 < 0) && (10 / 2 == 5) /* whatever */
        ) && 
        (
            (8 % 3 != 2) || /* supposed to write semantic analysi */
            (3 + 4 * 2 < 15 && 7 - 5 >= 1) // procrastinating instead...
        ) || 
        (
            (2 + 2 * 2 == 6) && (5 > 3 || 4 <= 1) // may Ozzy forgive me for this
) ||                                                        
        (
            (9 / 3) * (2 + 1) >= 9 && !(0 && 1) /* you may even call this... paw-thetic */
        ); 
}   


// oooh a comment
// at the edge of the world 
/*

digital footprint?
more like
digital paw-print :3
*/
